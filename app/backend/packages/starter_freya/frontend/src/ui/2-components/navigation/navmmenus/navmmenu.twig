{#
level: Ebene der Navigation, hier root, also 1
#}

{% set level = 1 %}

<div class="c-navmmenu">
    <ul class="nav level-{{ level }} justify-content-center">
        {% for item in items %}
            {% include '@component/navigation/navmmenus/_navmmenu-item/navmmenu-item.twig' with {
                item: item,
                level: level
            } only %}
        {% endfor %}
    </ul>
</div>
