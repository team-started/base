{#
level: level der Navi, hier root, also 1
#}

{% set level = 1 %}



<div class="c-navaccmenu">
    <ul class="level-{{ level }} list-unstyled">
        {% for item in items %}
            {% include '@component/navigation/navaccmenus/_navaccmenu-item/navaccmenu-item.twig' with {
                item: item,
                level: level
            } only %}
        {% endfor %}
    </ul>
<div>
