{#
identifier: zusammengesetzter String
uid:        Der Navigation kann optional eine uid übergeben werden, z.B. wenn
            - es verschiedene Sidebarnavigationen gibt
            - eine Navigation in einem Content-Element verwendet wird
#}

{% set identifier = 'metamenu' %}

<nav>
    <ul id="{{ identifier }}{{ uid }}" class="c-{{ identifier }} {{ additionalClass }}">
        {% for item in items %}
            {% include '@component/navigation/metamenus/_metamenu-item/metamenu-item.twig' with {
                identifier: identifier,
                menuUid: uid,
                item: item,
            } only %}
        {% endfor %}
    </ul>
</nav>
