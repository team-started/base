{#
siehe src/ui/2-components/accordions/accordion.twig

assets:         ein oder meherere Bilder
bodytext:       Für ein Item: der Text
grid:           Breite des Bildbereiches neben dem Text
imageorient:    Bildausrichtung (inside, beside, links, rechts)

Ob es ein Bild, Text oder beides gibt, regelt die Komponente textmedia.
#}

<div id="collapse{{ accordionItem.itemUid }}"
     class="accordion-collapse collapse"
     aria-labelledby="heading{{ accordionItem.itemUid }}"
     data-bs-parent="{{ toggle|default(true) ? '#' ~ uid }}">

    <div class="accordion-body">
        {% include '@component/textmedias/textmedia.twig' with {
            assets: accordionItem.assets,
            bodytext: accordionItem.text,
            grid: accordionItem.grid,
            imageorient: accordionItem.imageorient,
        } only %}
    </div>

</div>
