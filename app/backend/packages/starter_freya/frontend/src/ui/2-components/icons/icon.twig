{#
CType:              meint in diesem Fall 'ComponentType'
textMargin:         Abstand zwischen Bild und Text
additionalClass:    hier können Klassen für das Templating übergeben werden
#}

{% set CType = 'icon' %}
{% if text %}
    {% if layout == 'text-right' %}
        {% set layout = 'align-items-center' %}
        {% set textMargin = 'ms-2' %}
    {% endif %}
    {% if layout == 'text-bottom' %}
        {% set layout = 'flex-column align-items-center' %}
        {% set textMargin = 'mt-2' %}
    {% endif %}
    {% if layout == 'text-left' %}
        {% set layout = 'flex-row-reverse align-items-center' %}
        {% set textMargin = 'me-2' %}
    {% endif %}
    {% if layout == 'text-top' %}
        {% set layout = 'flex-column-reverse align-items-center' %}
        {% set textMargin = 'mb-2' %}
    {% endif %}
    {% set layout = 'd-flex ' ~ layout %}
{% endif %}
{% set link = link|merge({ 'additionalClass': layout }) %}

<div class="c-{{ CType }} {{ additionalClass }}">
    {% embed '@object/links/link.twig' with link %}
        {% block linkContent %}
            <span>
                {% include '@object/images/image.twig' with image only %}
            </span>
            {% if text %}
                <span class="{{ textMargin }}">
                    {{ text }}
                </span>
            {% endif %}
        {% endblock %}
    {% endembed %}
</div>
