{#
containerContent:   Enthält den jeweilige Inhalt des Containers.
containerId:        Im Regelfall ist die uid die uid des Contentelements.
                    Es gibt aber Fälle, in denen es kein Contentelement ist, z.B. ein Breadcrumb.
                    In diesem Fall wird der identifier der Komponente genommen.

Wenn es einen Hintergrund über volle Breite gibt, wird um den Container ein zusätzlicher Wrapper gesetzt.
#}

{% set containerId = uid ? 'con' ~ uid : identifier %}

{% set containerContent %}
    <div id="{{ containerId }}" class="{{ tx_starter_container|default('container') }} container-{{ CType|default('no-type') }} {{ tx_starter_visibility }} {{ space_before_class }} {{ space_after_class }}">
        {% block moduleContentBlock %}{% endblock %}
    </div>
{% endset %}

{% if tx_starter_background_fluid and tx_starter_background %}
    <div class="{{ tx_starter_background }} {{ tx_starter_visibility }}">
        {{ containerContent }}
    </div>
{% else %}
    {{ containerContent }}
{% endif %}
