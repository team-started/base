{#
Der Text wird im Context von Twig übergeben, daher scheint dies in Fractal leer.

{% block link %}: ist nötig, weil Bilder embeded werden: src/ui/1-objects/pictures/picture.twig
Bedeutet: Anstelle des Blocks wird das Bild eingesetzt.
#}

{% set target = config.target ? 'target="' ~ config.target ~ '"' %}
{% set title = config.title ? 'title="' ~ config.title ~ '"' %}
{% set class = config.class ? 'class="' ~ config.class ~ '"' %}


{% if config.uri %}
    <a href="{{ config.uri }}" {{ target }} {{ title }} {{ class }}">
        {% block link %}{{ text }}{% endblock %}
    </a>
{% else %}
    <span class="{{ config.class }}">
        {% block link %}{{ text }}{% endblock %}
    </span>
{% endif %}
